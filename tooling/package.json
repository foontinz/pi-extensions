{
  "name": "pi-tooling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone maintenance/benchmark scripts (run with npx tsx), not pi extensions.",
  "scripts": {
    "bench": "tsx bench-models.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.81.1",
    "@earendil-works/pi-coding-agent": "^0.81.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
